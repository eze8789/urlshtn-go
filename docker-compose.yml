version: '3'
services:
  postgres_db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: '123Change'
      POSTGRES_USER: 'local-user'
      POSTGRES_DB: 'url_shortener'
    ports:
      - 5432:5432